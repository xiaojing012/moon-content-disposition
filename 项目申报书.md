# moon-content-disposition 项目申报书

## 基本信息

- 项目名称：moon-content-disposition —— MoonBit 的 Content-Disposition 解析与安全工具库
- 参赛者：宋晓静
- 联系方式：2669550012@qq.com
- GitHub 仓库地址：https://github.com/xiaojing012/moon-content-disposition
- 项目方向：MoonBit HTTP 协议处理库 / Web 安全工具
- 是否为移植项目：否（依据 RFC 标准独立实现）

## 项目简介

Content-Disposition 是 HTTP 中控制文件下载名称的关键头字段，广泛出现在文件下载、表单上传和邮件附件的场景里。MoonBit 生态目前缺少一个严谨处理该字段的库，Web 开发者只能手工拼接字符串或做不完整的正则解析，文件名乱码、路径穿越、头注入等问题很容易被放过。

本项目实现 RFC 6266 Content-Disposition 的完整处理链路：解析 → 文件名解析 → 净化 → 审计，并支持 RFC 8187 国际文件名（`filename*`）。全部代码为 MoonBit 原生编写，不依赖任何第三方包，在 native、js、wasm-gc 三个目标上一致通过 327 个测试。

## 项目方向与适用场景

- 项目方向：MoonBit HTTP 协议处理与 Web 安全基础库。
- 适用场景：MoonBit Web 框架（文件下载、multipart/form-data 解析）、静态文件服务器、HTTP 客户端库、CLI 工具、浏览器端（wasm/js 目标）下载处理，以及需要把不可信的文件名写入文件系统的任何程序。

## 拟实现的核心功能（已完成）

- RFC 6266 §4.2 解析器：严格模式 + 兼容模式（5 种可记录的宽松恢复），结构化错误模型（13 个阶段、22 种错误、字节偏移与上下文摘录）；
- RFC 8187 `filename*` 解析与序列化：UTF-8 / ISO-8859-1 字符集、严格百分号解码、语言标签校验、`filename*` 优先规则；
- 文件名解析与净化：resolve_filename 选择下载名；sanitize_filename 按 Portable / WindowsLike / PosixLike 三种策略生成单个安全路径分量，防 `../`、防 Windows 保留名、防尾随点空格，非空输入永不失败；
- 安全审计：audit_header 报告路径分隔符、控制字符、危险扩展名、重复参数等 18 类问题并分级；
- 序列化与生成：字节级确定性重序列化、幂等规范化、generate_attachment / generate_inline 反向生成，CR/LF/NUL 注入一律拒绝；
- 资源限制：7 项独立 Limits（输入字节、参数个数、值长度等），超限报错而非静默截断；
- 工具链：11 个命令的 JSON 输出 CLI（disposition-tool）和 6 个可运行示例。

## 交付物与功能边界

做：RFC 6266 / RFC 8187 的解析与序列化、filename 的 resolve / sanitize / audit、CLI 工具与示例。
不做：HTTP server / client、真实的文件写入、病毒检测等，这些留给上层应用。

## 技术路线

Parser → Model → Resolver → Sanitizer / Audit → Serializer / Generator 的流水线，各阶段为纯函数，输出即下一阶段输入。两条横切保障：结构化错误模型（阶段、类型、字节偏移、上下文摘录），以及 7 项独立 Limits 保证资源有界。同一套代码在 native、js、wasm-gc 三个目标构建通过。

## 测试与可复现性

327 个命名测试覆盖三个目标全部通过；确定种子 PRNG 驱动的属性测试共 2600+ 用例、1300 条净化不变量、600 条规范化用例；scripts/verify_all.ps1 一键完成三目标端到端验证。

## 风险与应对

- 不可信文件名：默认按不受信输入处理，审计先于落盘；
- 路径穿越与 Windows 保留名：净化策略强制单路径分量；
- CR/LF 注入：生成端直接拒绝控制字节；
- 跨平台文件名差异：三种 Profile 分别覆盖 Portable / Windows / Posix；
- 宽松兼容需求：严格模式为主，兼容模式 5 种恢复逐条记录可审计。

## 原创性说明

本项目为原创实现，不属于移植项目。实现依据是 RFC 6266、RFC 8187、RFC 7230、RFC 5987、RFC 5646 等公开标准文档（标准文本为规范而非代码，未复制任何源码）。测试用例中的 RFC 示例来自标准附录，已在 THIRD_PARTY_NOTICES 中逐条声明。许可证为 Apache-2.0。
